import 'package:flutter/material.dart';

class AddBlogScreenContent extends StatelessWidget {
  const AddBlogScreenContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Add Blog'),
      ),
    );
  }
}
